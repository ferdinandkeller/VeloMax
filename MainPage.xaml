<Page
    x:Class="VéloMax.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VéloMax"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <NavigationView x:Name="Navview" IsBackButtonVisible="Collapsed" IsSettingsVisible="False">
        <NavigationView.Resources>
            <AcrylicBrush x:Key="NavigationViewDefaultPaneBackground"
                        BackgroundSource="HostBackdrop"
                        FallbackColor="{StaticResource SystemChromeLowColor}"/>
            <AcrylicBrush x:Key="NavigationViewExpandedPaneBackground"
                        BackgroundSource="HostBackdrop"
                        FallbackColor="{StaticResource SystemChromeLowColor}"/>
        </NavigationView.Resources>
        <NavigationView.Content>
            <Grid  Padding="40">
                <TextBlock Text="J'aime les frites" Style="{StaticResource TitleTextBlockStyle}" />
                <Button Content="azdauizdhiauzdh" />
            </Grid>
        </NavigationView.Content>
    </NavigationView>
</Page>
