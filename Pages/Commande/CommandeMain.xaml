<Page
    x:Class="VéloMax.Pages.CommandeMain"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VéloMax.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <NavigationView x:Name="NavViewCommandes" PaneDisplayMode="Top" IsBackButtonVisible="Collapsed" IsSettingsVisible="False" ItemInvoked="Navview_ItemInvoked">
            <NavigationView.Resources>
                <!--<AcrylicBrush x:Key="NavigationViewDefaultPaneBackground"
                            BackgroundSource="Backdrop"
                            TintColor="Transparent"
                            FallbackColor="White"/>
            <AcrylicBrush x:Key="NavigationViewExpandedPaneBackground"
                            BackgroundSource="HostBackdrop"
                            TintColor="{StaticResource SystemRevealChromeWhiteColor}"
                            TintOpacity="1"
                            FallbackColor="White"/>-->
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItemHeader Content="Commandes"/>
                <NavigationViewItem Tag="commande1" Content="A Gérer"/>
                <NavigationViewItem Tag="commande2" Content="En Cours" />
                <NavigationViewItem Tag="commande3" Content="Envoyées" />
                <NavigationViewItem Tag="commande4" Content="Ajouter" />
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <Frame Name="NavigationContentFrame" >

                </Frame>
            </NavigationView.Content>

        </NavigationView>
    </Grid>
</Page>
